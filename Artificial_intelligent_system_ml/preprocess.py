import pandas as pd

def main():
    print("Starting preprocessing...")
    """
    dataset_filename = "data_set\Company_Bankruptcy_Prediction.csv"
    
    # Carico il dataset
    raw_data = pd.read_csv(dataset_filename)
    
    df = pd.DataFrame(raw_data)
    df.to_csv(dataset_filename, index=False)
    """
